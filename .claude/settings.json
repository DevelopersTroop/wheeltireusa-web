{
  "permissions": {
    "allow": [
      "Bash(rm -rf components hooks)",
      "Bash(mkdir -p CategoryToggle VehicleDropdowns GoButton DesktopView MobileView)",
      "mcp__context7__query-docs",
      "Bash(npx tsc:*)"
    ]
  }
}
